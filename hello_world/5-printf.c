#include <stdio.h>

/**
 * main - fonction to run program
 * Return: 0
 */

int main(void)
{
	char bla[] = "\"with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", bla);

	return (0);
}
