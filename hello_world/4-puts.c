#include <stdio.h>

/**
 * main - fonction
 * str - array to print
 * Return: 0
 */

int main(void)
{

	char str[] = "\"Programming is like building a multilingual puzzle\n";

	puts(str);

	return (0);
}
